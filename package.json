{
  "name": "kuraberu-ikuji",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.11.1",
  "engines": {
    "node": "^20.19.1 || >=22.12.0",
    "pnpm": "10.11.1"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "format": "node node_modules/.pnpm/prettier@3.9.6/node_modules/prettier/bin/prettier.cjs --write .",
    "format:check": "node node_modules/.pnpm/prettier@3.9.6/node_modules/prettier/bin/prettier.cjs --check .",
    "lint": "eslint .",
    "typecheck": "astro check",
    "test": "vitest run",
    "validate:env": "node scripts/validate-build-env.mjs",
    "validate:content": "node scripts/validate-content.mjs",
    "check:rendered": "node scripts/check-rendered-html.mjs",
    "check:deployment": "node scripts/check-deployment-html.mjs",
    "check:external-link-syntax": "node scripts/check-external-links.mjs",
    "check:external-links": "npm run check:external-link-syntax",
    "verify": "npm run format:check && npm run lint && npm run typecheck && npm run validate:env && npm run validate:content && npm run build && npm run check:rendered && npm run check:deployment && npm run check:external-link-syntax && npm run test"
  },
  "dependencies": {
    "astro": "6.0.3",
    "typescript": "5.9.3"
  },
  "devDependencies": {
    "@astrojs/check": "latest",
    "eslint": "latest",
    "prettier": "latest",
    "vitest": "latest"
  },
  "pnpm": {
    "overrides": {
      "cookie": "1.0.2"
    }
  }
}
